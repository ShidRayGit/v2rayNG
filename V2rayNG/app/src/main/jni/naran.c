/*
 * کلید API در قالب بایت‌های XOR شده نگه داشته می‌شود تا با `strings` روی
 * فایل .so مستقیم پیدا نشود. این کار حرفه‌ای‌ها را متوقف نمی‌کند، فقط
 * برداشتن سرسری را غیرممکن می‌کند.
 *
 * ساخت SECRET_BYTES:  python3 tools/gen_secret.py <API_SECRET>
 */
#include <jni.h>
#include <string.h>

static const unsigned char MASK[] = { 0x5A, 0xC3, 0x17, 0x8E, 0x2D, 0x71, 0xB4, 0x66 };

/* جای این آرایه را با خروجی gen_secret.py عوض کنید */
static const unsigned char SECRET_BYTES[] = {
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00
};

JNIEXPORT jbyteArray JNICALL
Java_com_naran_core_NaranCrypto_apiSecret(JNIEnv *env, jobject thiz) {
    const size_t n = sizeof(SECRET_BYTES);
    unsigned char out[sizeof(SECRET_BYTES)];
    for (size_t i = 0; i < n; i++) {
        out[i] = SECRET_BYTES[i] ^ MASK[i % sizeof(MASK)];
    }
    jbyteArray arr = (*env)->NewByteArray(env, (jsize)n);
    (*env)->SetByteArrayRegion(env, arr, 0, (jsize)n, (const jbyte *)out);
    memset(out, 0, n);
    return arr;
}
